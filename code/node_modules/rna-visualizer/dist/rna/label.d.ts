import { Vector2, Residue, Line, Text, Transformation } from './data-structures';
export declare class Label {
    residue: Residue;
    line: Line;
    text: Text;
    private visible;
    constructor(residue: Residue, line: Line, text: Text);
    setTransform(transform: Transformation): Label;
    setVisible(visible: boolean): Label;
    isVisible(): boolean;
    translate(shift: Vector2): Label;
}
