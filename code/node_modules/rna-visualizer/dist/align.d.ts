import DataContainer from './dataContainer';
import { Residue } from './rna/data-structures';
export declare class Group {
    xShift: number;
    yShift: number;
    members: Residue[];
    constructor(x: number, y: number, member: Residue);
    push(member: Residue): void;
    size(): number;
    has(index: number): boolean;
}
export declare function createGroups(contA: DataContainer, contB: DataContainer, group?: Group, filter?: number): Array<Group>;
export declare function getBestGroup(groups: Group[]): Group;
