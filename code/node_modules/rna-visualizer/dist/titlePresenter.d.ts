import { Styles } from './classes';
import { Residue, Rectangle, Text } from './rna/data-structures';
export declare class Title {
    private texts;
    private background;
    private styles;
    private visible;
    constructor(texts: Text[], background: Rectangle, styles: Styles);
    getTexts(): Text[];
    getBackground(): Rectangle;
    draw(ctx: CanvasRenderingContext2D): void;
    static fromResidues(residues: Residue[], canvasWidth: number, canvasHeight: number, styles: Styles): Title;
}
